---
description: Show launcher project status — what's built, what's next
---
Read `docs/spec.md` and summarize:
1. Which features are completed (marked ✅)
2. Which features are still pending
3. What the next feature to implement is
4. Try building with `./gradlew assembleDebug` and report if it compiles
